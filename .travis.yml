language: java
before_install:
  - sudo apt-get -qq update
  # ant-optional contains Ant JUnit tasks
  - sudo apt-get install ant-optional
install: true
script: ant
jdk:
  - openjdk8
  - openjdk9
  - openjdk11
